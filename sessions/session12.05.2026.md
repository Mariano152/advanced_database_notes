# Session - 2026-05-12

## Topics covered
- SQLAlchemy ORM models.
- Relationships between tables using `relationship()` and `ForeignKey`.
- Alembic migrations with `command.revision()` and `autogenerate=True`.
- Basic CRUD operations using SQLAlchemy sessions.
- Rolling back migrations with `command.downgrade()`.

## What I understood
- I understood that ORM models represent database tables as Python classes. Relationships help connect objects in Python, like a task having comments or a user belonging to a team. Alembic migrations are useful because they track schema changes instead of changing the database manually every time.

## What is still confusing

## Questions


## Related concepts


## Resources used
- See `sql_challenges/challenge-11/`
